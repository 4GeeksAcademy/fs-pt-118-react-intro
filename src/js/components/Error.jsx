
import React from "react";
//const myError ---> no sirve, tiene que ser PascalCase
const MyError = () => {

    return(
        <div>
            esto nos rive porque el nombre del componente, que es el nombre de la funcion, esta en minuscula
        </div>
    )
}

export default MyError